<%= render @gratitude %>

<div class="mt-3">
  <%= link_to "Edit", edit_gratitude_path(@gratitude), class: "btn btn-outline-primary" %>
  <%= link_to "Back to gratitudes", gratitudes_path, class: "btn btn-outline-secondary" %>
  <%= button_to "Delete", @gratitude, method: :delete, class: "btn btn-outline-danger", data: { turbo_confirm: "Are you sure?" } %>
</div>
